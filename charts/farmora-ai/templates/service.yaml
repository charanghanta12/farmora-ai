apiVersion: v1
kind: Service
metadata:
  name: {{ include "farmora-ai.fullname" . }}
  labels:
    {{- include "farmora-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "farmora-ai.selectorLabels" . | nindent 4 }}
